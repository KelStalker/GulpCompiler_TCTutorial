// Every time we set up typescript, it helps to have this config file for that
// How we want to compile - just basic options here - much more on website
// being a json file, we need to follow the json format
{
    "compilerOptions": {
        "outFile": "js/scripts.js"
    },
    // Include any files insider the src/ts/ folder by "**" 
    "include": ["src/ts/**/"],
}

// Next step is to type (in bash console) "$ npm init" in bash (not powershell or terminal)
// enter "package name: (gulp) gulp-example
// version: (1.0.0)
// description: A Gulp-enabled project for compiling SASAA and TypeScript
// Entry point: (index.js) gulpfile.js
// test command:
// git repository: (paste repository here)
// keywords: Gulp, TypeScript, TS,m SASS. scss
// author: TC video tutorial by Manpower